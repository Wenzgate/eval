<div class="movie-item">
    <h2>{{ item.title }}</h2>
    <video controls>
        <source src="{{ item.value }}" type="video/mp4">
        Your browser does not support the video tag.
    </video>
</div>